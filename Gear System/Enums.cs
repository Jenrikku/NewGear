namespace NewGear.GearSystem.Enums {
    public enum EditorEntries {
        None = default,
        FileTree,
        HexView,
        Viewport
    }

    public enum DialogEntries {
        FileSelector
    }
}
